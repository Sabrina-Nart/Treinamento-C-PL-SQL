
        /*
        Console.WriteLine("Digite sua idade: ");
        int? idade = int.Parse(Console.ReadLine());

        //Console.WriteLine("Sua idade é: ");
        //Console.WriteLine(idade);

        if (idade < 18)
        {
            //Console.WriteLine("Você é menor de idade e não pode dirigir! - Sua idade é: {0} + {1} + {2}", idade, idade, 23);
            Console.WriteLine("Você é menor de idade e não pode dirigir! - Sua idade é: " + idade + "\n"); 
        } else if (idade > 18)
        {
            Console.WriteLine("Você é maior de idade e pode dirigir");
        } else
        {
            Console.WriteLine("Você está na carteira provisória");
        }
        *
        //---------------------------------------------------------------------
        
        Console.WriteLine("Digite uma valor: ");
        decimal valor = decimal.Parse(Console.ReadLine());

        Console.WriteLine("Seu valor é: ");
        Console.WriteLine(valor);
        
        //----------------------------------------------------------------------
        /*
        if
        for    // Para cada coisa que acontecer, vou fazer outra coisa
        while  // Enquanto uma situação for verdadeira, vou fazer outra coisa
        switch //Para determinada informação, caso seja um valor, vou ver uma coisa
        foreach //Para cada elemento dentro de uma lista - relação de vários objetos
        */
        //

        // < > = ! <= >=

